/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.icons8.com",
      "encrypted-tbn0.gstatic.com",
      "media.istockphoto.com",
    ],
  },
};

export default nextConfig;
